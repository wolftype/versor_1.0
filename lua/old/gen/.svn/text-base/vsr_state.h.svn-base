namespace vsr {

template< type T, int NUM, int IDX>
class State {
	
	T w[NUM];
	int mIdx = IDX;
	
	State (const State& )
	
	
	template< int INUM, int IIDX, RNUM, int RIDX >
	State < RNUM, RIDX > operator * ( State< INUM, IDX  > );
	
};


}